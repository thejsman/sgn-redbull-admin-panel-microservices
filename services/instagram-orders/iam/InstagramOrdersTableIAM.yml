InstagramOrdersTableIAM:
  Effect: "Allow"
  Action:
    - dynamodb:Query
    - dynamodb:GetItem
    - dynamodb:PutItem
    - dynamodb:UpdateItem
    - dynamodb:DeleteItem
    

  Resource:
    - arn:aws:dynamodb:${self:provider.region}:*:table/${self:provider.environment.INSTAGRAM_ORDERS_TABLE}
    - arn:aws:dynamodb:${self:provider.region}:*:table/${self:provider.environment.INSTAGRAM_ORDERS_TABLE}/index/*
